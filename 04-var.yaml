- name: play level var
  hosts: local
  connection: local
  vars:
    course: "devops"
    duration: "140 hrs"
    trainer: "priya"
  task:
  - name: print variable
    ansible.builtin.debug:
      msg: "the sourse: {{ course }}, trainer is: {{ trainer }}"